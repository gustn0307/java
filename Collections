interface Collection
Collection 인터페이스를 구현해서 class를 구현체이다
무한으로 데이터를 관리하는 집합구조
1. List 계열
 	주요클래스: ArrayList, Vector, LinkedList
 	1. 데이터의 중복사용 가능
 	2. 인덱스 번호가 0부터 자동 부여되고, 인덱스 번호로 해당 데이터에 접근가능
 	3. Vector는 synchronized 메소드로
 	   LinkedList 빈번한 데이터 삽입
 	4. add(데이터), add(인덱스,데이터): 삽입, 중간삽입
 	5. set(인덱스), set(인덱스,데이터): 수정
 	6. get(인덱스): 읽기
 	7. remove(인덱스): 삭제
 	
 	for(요소를 저장할 수 있는 변수:콜렉션 ){} JAVA5부터 활용가능한 for문
 	
 	
 2. Set 계열
 	대표적인 클래스: HashSet, TreeSet, LinkedSet
 	1. 데이터의 중복불가
 	2. 순서를 유지하지 않고 저장(인덱스가 없음)
 	3. add(데이터): 삽입
 	4. remove(데이터): 삭제
 	5. Iterator를 통해 데이터 추출가능(forEach문도 가능)
 	6. TreeSet은 default로 오름차순 정렬
 	7. LinkedSet은 
 	
 	  	
 3. Map 계열: key
 	
